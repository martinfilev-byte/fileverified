#!/bin/bash
# push.sh - Автоматизирано качване със Snapshot

# 1. Генерираме новия снапшот
echo "🚀 Стартирам генериране на snapshot..."
chmod +x snapshot.sh
./snapshot.sh

# 2. Добавяме всички промени към Git
echo "📦 Индексирам промените..."
git add .

# 3. Commit-ваме (ако не подадеш съобщение, използва автоматично с дата)
MSG=${1:-"Update: $(date +'%Y-%m-%d %H:%M:%S') with project snapshot"}
git commit -m "$MSG"

# 4. Push към GitHub
echo "⬆️ Изпращам към GitHub..."
git push origin main

echo "✨ Всичко е качено! Можеш да ми дадеш линка към project_snapshot.txt"